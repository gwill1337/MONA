apiVersion: v1
kind: Service
metadata:
  name: loki-stack-nodeport
  namespace: mona
spec:
  type: NodePort
  selector:
    app: loki
    release: loki-stack
  ports:
  - port: 3100
    targetPort: 3100
    nodePort: 30100
    protocol: TCP
