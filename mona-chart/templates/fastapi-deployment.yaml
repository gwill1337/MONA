---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mona-fastapi
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.fastapi.replicas }}
  selector:
    matchLabels:
      app: fastapi
  template:
    metadata:
      labels:
        app: fastapi
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "{{ .Values.fastapi.service.targetPort }}"
    spec:
      automountServiceAccountToken: {{ .Values.fastapi.automountServiceAccountToken }}
      securityContext:
        {{- toYaml .Values.fastapi.podSecurityContext | nindent 8 }}
      initContainers:
        - name: wait-for-postgres
          image: busybox:1.36
          imagePullPolicy: Always
          resources:
            requests:
              memory: "16Mi"
              cpu: "10m"
            limits:
              memory: "32Mi"
              cpu: "50m"
          command: ['sh', '-c']
          args:
            - |
              until nc -z -w 2 postgres {{ .Values.postgres.service.port }}; do
                echo "Waiting for PostgreSQL...";
                sleep 2;
              done;
              echo "PostgreSQL is available!"
        - name: alembic-upgrade
          image: "{{ .Values.images.fastapi.repository }}:{{ .Values.images.fastapi.tag }}"
          imagePullPolicy: {{ .Values.images.fastapi.pullPolicy }}
          resources:
            {{- toYaml .Values.fastapi.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.fastapi.initContainerSecurityContext | nindent 12 }}
          command: ["alembic", "upgrade", "head"]
          env:
            {{- range $key, $val := .Values.fastapi.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            - name: POSTGRES_USER
              value: {{ .Values.postgres.auth.user | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.postgres.auth.password | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.postgres.auth.db | quote }}
            - name: POSTGRES_HOST
              value: postgres
            - name: POSTGRES_PORT
              value: {{ .Values.postgres.service.port | quote }}
      enableServiceLinks: false
      containers:
        - name: fastapi
          image: "{{ .Values.images.fastapi.repository }}:{{ .Values.images.fastapi.tag }}"
          # checkov:skip=CKV_K8S_15:
          imagePullPolicy: {{ .Values.images.fastapi.pullPolicy }}
          ports:
            - containerPort: {{ .Values.fastapi.service.targetPort }}
          livenessProbe:
            httpGet:
              path: {{ .Values.fastapi.livenessProbe.httpGet.path }}
              port: {{ .Values.fastapi.service.targetPort }}
            initialDelaySeconds: {{ .Values.fastapi.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.fastapi.livenessProbe.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.fastapi.readinessProbe.httpGet.path }}
              port: {{ .Values.fastapi.service.targetPort }}
            initialDelaySeconds: {{ .Values.fastapi.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.fastapi.readinessProbe.periodSeconds }}
          securityContext:
            {{- toYaml .Values.fastapi.containerSecurityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.fastapi.resources | nindent 12 }}
          env:
            {{- range $key, $val := .Values.fastapi.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            - name: POSTGRES_USER
              value: {{ .Values.postgres.auth.user | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.postgres.auth.password | quote }}
            - name: POSTGRES_DB
              value: {{ .Values.postgres.auth.db | quote }}
            - name: POSTGRES_HOST
              value: postgres
            - name: POSTGRES_PORT
              value: {{ .Values.postgres.service.port | quote }}

---

apiVersion: v1
kind: Service
metadata:
  name: fastapi
  namespace: {{ .Release.Namespace }}
  labels:
    app: fastapi
spec:
  type: {{ .Values.fastapi.service.type }}
  selector:
    app: fastapi
  # ports:
  #   - port: {{ .Values.fastapi.service.port }}
  #     targetPort: {{ .Values.fastapi.service.targetPort }}
  #     nodePort: {{ .Values.fastapi.service.nodePort }}
  ports:
  - name: http
    port: {{ .Values.fastapi.service.port }}
    targetPort: {{ .Values.fastapi.service.targetPort }}
    nodePort: {{ .Values.fastapi.service.nodePort }}
